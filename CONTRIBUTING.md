# Contributing

- Keep changes small and testable.
- Reuse existing backend services and gateway paths before adding new abstractions.
- Run backend tests, frontend lint, frontend build, and Docker build before opening a PR.
- Never commit secrets, generated artifacts, or local environment files.
