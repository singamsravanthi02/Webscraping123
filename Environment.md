# Environment

- Backend env vars live in `backend/.env.example`.
- Frontend env vars live in `frontend/.env.example`.
- Never commit real secrets.
- Use separate values for local, staging, and production.
